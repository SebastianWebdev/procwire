---
title: Procwire
description: High-performance IPC library for Node.js with binary data plane protocol
template: splash
hero:
  tagline: High-performance IPC for Node.js. Binary protocol for the data plane, JSON-RPC for control.
  image:
    file: ../../assets/houston.webp
  actions:
    - text: Get Started
      link: /guides/getting-started/
      icon: rocket
      variant: primary
    - text: View Architecture
      link: /guides/architecture/
      icon: document
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

<div className="landing-section">

## Why Procwire?

<CardGrid>
  <Card title="Binary Data Plane" icon="rocket">
    Binary wire protocol with an 11-byte header for the data plane. **Target: >1 GB/s throughput** for large payloads.
  </Card>

<Card title="Dual-Channel Architecture" icon="setting">
  Control plane (stdio, JSON-RPC) for lifecycle management. Data plane (pipe, binary) for
  high-throughput user data.
</Card>

<Card title="Type-Safe" icon="approve-check">
  Full TypeScript support with builder pattern and generics. Schema-first design where parent
  defines the contract.
</Card>

<Card title="Zero Dependencies" icon="puzzle">
  Core packages have zero runtime dependencies. Optional codec packages for MessagePack, Arrow, and
  more.
</Card>

<Card title="Cross-Platform" icon="laptop">
  Named Pipes on Windows, Unix Domain Sockets on Linux/macOS. One API, all platforms.
</Card>

  <Card title="High Throughput" icon="star">
    A binary data plane instead of JSON-RPC delivers an ~80x throughput improvement for large payloads.
  </Card>
</CardGrid>

</div>

<div className="landing-section" />

## Dual-Channel Design

Procwire splits control from data so each channel uses the right protocol:

| Channel       | Protocol   | Profile               |
| ------------- | ---------- | --------------------- |
| Control Plane | JSON-RPC   | small, infrequent     |
| Data Plane    | **Binary** | ~30 MB/s -> ~2.5 GB/s |

### Why a binary data plane?

JSON-RPC suits the small, infrequent control messages, but it would cripple the
data plane:

- **~5x larger payload** (JSON text vs binary)
- **~500ms serialization overhead** per 1MB of data

The binary wire format avoids this overhead entirely.

```
Binary Frame (Data Plane):
+----------+-------+----------+----------+----------------------+
| Method ID| Flags | Req ID   | Length   | Payload              |
| 2 bytes  | 1 byte| 4 bytes  | 4 bytes  | N bytes              |
+----------+-------+----------+----------+----------------------+
```
